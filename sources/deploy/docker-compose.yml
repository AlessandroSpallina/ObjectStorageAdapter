version: '3'
services:
        filemanagementservice:
                image: fms:v0
                restart: always
                links:
                        - db
                ports:
                        - "8080:8080"
                environment:
                        MYSQL_ROOT_PASSWORD: root
        db:
                image: mariadb
                restart: always
                environment:
                        MYSQL_ROOT_PASSWORD: root
